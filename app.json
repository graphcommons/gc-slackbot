{
  "name": "gc-slackbot",
  "description": "A slackbot for graphing public activity on Graph Commons",
  "repository": "https://github.com/artsince/gc-slackbot",
  "keywords": ["node", "slack", "botkit", "graph commons"],
  "env": {
    "SLACK_TOKEN": {
      "description": "Secret token for the Slack Bot",
      "value": "",
      "required": true
    },
    "GC_TOKEN": {
      "description": "Graph Commons API key",
      "value": "",
      "required": true
    },
    "GRAPH_ID": {
      "description": "Use and existing graph id on Graph Commons. Beware that, a new graph will be created on every startup if left blank.",
      "value": "",
      "required": false
    },
    "DEBUG": {
      "description": "Run bot in debug mode",
      "value": "false",
      "required": false
    }
  }
}
